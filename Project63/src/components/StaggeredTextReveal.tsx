import { motion } from 'framer-motion';

const StaggeredTextReveal = () => {
    return (
        <div>
            <motion.div>

            </motion.div>
        </div>
    )
}

export default StaggeredTextReveal
